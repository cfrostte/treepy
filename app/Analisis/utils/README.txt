Developed under:
	OS: Ubuntu 16.04 LTS
	Language: Python 3.5.2

Required packages:
        sudo apt-get install python3-pip
        sudo apt-get install python3-tk

Required PIP modules:
	Please use requirements.txt to install the packages
	pip install -r requirements.txt

#------------------------------------------------

Image:
	- arboles_020.jpg
		* minarea = 30
		* min_dist = 40
		* umbra = [(0,45), (21,65), (11,63)]
	- arboles_00.jpg
		* minarea = 110
		* min_dist = 80
		* umbra = [(2,62), (24,84), (4,84)]
